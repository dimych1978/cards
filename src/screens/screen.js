function renderEasyScreen() {
  cards.textContent = '';
  const title = document.createElement('div');
  title.textContent = 'title placeholder';
  const content = document.createElement('div');
  content.textContent = 'easy placeholder';
  // window.application.renderBlock('easyBlock', content);

  cards.appendChild(title);
  cards.appendChild(content);
}

function renderNormalScreen() {
  cards.textContent = '';
  const title = document.createElement('div');
  title.textContent = 'title placeholder';
  const content = document.createElement('div');
  content.textContent = 'normal placeholder';
  // window.application.renderBlock('normalBlock', content);

  cards.appendChild(title);
  cards.appendChild(content);
}

function renderHardScreen() {
  cards.textContent = '';
  const title = document.createElement('div');
  title.textContent = 'title placeholder';
  const content = document.createElement('div');
  content.textContent = 'hard placeholder';
  // window.application.renderBlock('hardBlock', content);

  cards.appendChild(title);
  cards.appendChild(content);
}
